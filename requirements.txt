# Core
torch>=2.0
numpy>=1.24
# Wavelet (kymatio) needs sph_harm from scipy.special; use scipy 1.13.x until kymatio catches up.
# SpatioTemporalGNN: models/fmgreco_stnn/graph_utils.py kNN (cKDTree) for large N; keep scipy installed on cluster images.
scipy>=1.11,<1.14

# Used by several submissions
einops
huggingface_hub

# Gated EGNO (PR #10): neuralop.SpectralConv + torch_scatter
neuraloperator>=2.0
# torch_scatter: `pip install torch-scatter` (use PyG wheel URL matching your torch + CUDA/CPU)
torch-scatter>=2.1

# Zonal MoE (zonal_moe) — PyG (shares torch-scatter above)
torch-geometric
# DeltaGraph (PR #25): fps, knn, knn_graph — install wheel matching your torch + CUDA/CPU
torch-cluster

# Wavelet latent operator (PR #18)
kymatio>=0.3.0

# SpatioTemporalGNN steerable backbone (e3nn irreps + spherical harmonics)
e3nn>=0.5.0
